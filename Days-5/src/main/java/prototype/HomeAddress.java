package prototype;

public class HomeAddress extends AddressPrototype{

    public HomeAddress( String name, String city, String address, String no ) {
        setName(name);
        setCity(city);
        setAddress(address);
        setNo(no);
    }
}
