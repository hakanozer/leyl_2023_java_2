package prototype;

public class CompanyAddress extends AddressPrototype{

    public CompanyAddress(String name, String title, String email, String city, String address, String no) {
        setName(name);
        setTitle(title);
        setEmail(email);
        setCity(city);
        setAddress(address);
        setNo(no);
    }

}
